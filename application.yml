vendor:
  config:
    name: Prabu Murugesan Testing

server:
  servlet:
    context-path: /api/vendor
spring:
  application:
    name: vendor-microservice
  data:
    redis:
      host: localhost
      port: 6379
      ttl: 3600
  datasource:
    password: adming@123
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect








